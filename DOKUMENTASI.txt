===================================================================
DOKUMENTASI TEKNIS - UT Academic Tracker Bot
Versi: 6.0 | Terakhir diperbarui: 12 September 2026
===================================================================

STRUKTUR FILE PROYEK:
  task-tracker/
  ├── .github/workflows/jadwal-bot.yml  → Jadwal GitHub Actions (tiap 4 jam)
  ├── src/main/java/com/autotracker/
  │   ├── App.java                       → Main + orkestrasi semua pengecekan
  │   ├── MoodleService.java             → Semua API Moodle UT
  │   ├── NotionService.java             → Semua API Notion (state management)
  │   ├── TelegramService.java           → Kirim notifikasi & baca keyword Telegram
  │   └── DOKUMENTASI.txt               → File ini
  ├── .env                               → Kredensial lokal (JANGAN di-push!)
  ├── .env.example                       → Template .env
  ├── .gitignore                         → .env diabaikan Git
  ├── README.md                          → Dokumentasi publik (GitHub)
  └── pom.xml                            → Maven dependencies

===================================================================
FITUR AKTIF (v6.0)
===================================================================

[CEK 1] MATKUL BARU
  - API: core_enrol_get_users_courses
  - Cek apakah matkul sudah ada di Notion (by Name)
  - Simpan ke Notion dengan prefix [MATKUL]
  - Notif Telegram hanya saat ada matkul baru (awal semester)
  - Fitur Tambahan: Jika [STATUS] Stop sedang aktif di Notion dan matkul baru
    terdeteksi, bot otomatis melakukan auto-resume (menghapus [STATUS] Stop)
    dan mengirimkan notifikasi ke Telegram.

[CEK 2] TUGAS & KUIS
  - API: mod_assign_get_assignments
  - Filter: hanya assignment yang sudah dibuka (allowsubmissionsfromdate) dan
    belum melewati deadline (duedate)
  - Cek duplikat: Name + Mata Kuliah di Notion (prefix [TUGAS])
  - Cek completion via cmid (Activity Completion API)
  - Fallback: mod_assign_get_submission_status jika cmid tidak tersedia
  - Notif Telegram jika ada tugas baru yang belum dikerjakan

[CEK 3] DISKUSI FORUM
  - API 1: mod_forum_get_forums_by_courses       → Ambil semua forum
  - API 2: mod_forum_get_forum_discussions        → Ambil diskusi per forum
  - API 3: core_completion_get_activities_completion_status → Status selesai
  - Filter: hanya forum yang namanya diawali "diskusi", "kehadiran", atau "tugas"
  - Completion status dibaca dari Activity Completion Moodle (= tanda hijau Done)
  - Completion map dibangun SEKALI per course (efisien, bukan per diskusi)
  - Fallback: mod_forum_get_discussion_posts jika cmid tidak tersedia
  - Deteksi otomatis matkul Praktik (seksi "Aktivitas Belajar") vs Reguler (seksi "Sesi")

  Decision matrix tiap diskusi:
  ┌─────────────────────────────┬──────────────┬──────────────────────────────┐
  │ Kondisi                     │ Di Notion?   │ Aksi                         │
  ├─────────────────────────────┼──────────────┼──────────────────────────────┤
  │ State >= 1 (Done di Moodle) │ Sudah ada    │ Update Notion → "Selesai" ✅ │
  │ State = 0  (Belum)          │ Sudah ada    │ Skip (sudah dinotif)         │
  │ State >= 1 (Done di Moodle) │ Belum ada    │ Simpan langsung sbg Selesai  │
  │ State = 0  (Belum)          │ Belum ada    │ Simpan + Notif Telegram 🔔   │
  └─────────────────────────────┴──────────────┴──────────────────────────────┘

[CEK 4] PESAN DOSEN
  - Deteksi pesan private yang belum dibaca dari dosen di Moodle.
  - Kirim pengingat ke Telegram jika ada unread count > 0 dari orang lain.

[CEK 5 / TELEGRAM KEYWORD] TRIGGER WORD "Stop" & "Run"
  - Method: TelegramService.ambilDanAkuiKeyword() — satu panggilan untuk semua keyword.
  - Dibaca di awal setiap run, sebelum cek elearning.
  - Semua update di-ACKNOWLEDGE setelah dibaca (getUpdates?offset=max_id+1) agar
    pesan lama tidak muncul lagi di run berikutnya. Ini mencegah bug di mana bot
    mendeteksi keyword "Stop" lama bersamaan dengan keyword "Run" baru dalam satu run.
  - Keyword diproses secara BERURUTAN sesuai urutan pesan di chat.
    Perintah terakhir yang dikirim user adalah yang berlaku (last command wins).

  Keyword "Stop":
    - Jika diterima dan [STATUS] Stop belum ada di Notion:
      → Menyimpan penanda [STATUS] Stop ke database Notion
      → Mengirimkan konfirmasi ke Telegram
    - Efek: laporan periodik tidak akan dikirim selama [STATUS] Stop aktif
    - Bot TETAP berjalan dan mengecek elearning setiap run

  Keyword "Run":
    - Jika diterima dan [STATUS] Stop ada di Notion:
      → Menghapus (arsip) penanda [STATUS] Stop dari Notion
      → Mengirimkan konfirmasi ke Telegram
    - Efek: laporan periodik kembali dikirim seperti biasa


[PENANGANAN AKHIR SEMESTER]
  - Jika daftar mata kuliah di e-learning Moodle kosong (semester berakhir):
    1. Bot akan otomatis menghapus status [STATUS] Matkul Komplit di Notion.
    2. Bot langsung tidur (return) tanpa mengirimkan laporan periodik.
    3. [STATUS] Stop TIDAK dihapus saat tidur — tetap aktif sampai user kirim "Run"
       atau ada matkul baru terdeteksi (auto-resume).

[AUTO-RESUME MATKUL BARU]
  - Dipicu setelah cekMatkulBaru() jika adaMatkulBaru = true DAN [STATUS] Stop aktif.
  - Bot menghapus [STATUS] Stop dari Notion secara otomatis.
  - Mengirimkan notifikasi ke Telegram bahwa laporan diaktifkan kembali.
  - Dirancang untuk skenario: user kirim "Stop" di akhir semester,
    lalu semester baru dimulai → bot resume sendiri tanpa intervensi manual.

[LAPORAN] PERIODIK (kondisional)
  - Query Notion: semua item dengan Name starts_with "[DISKUSI]" dan "[TUGAS]"
  - Filter di Java: hanya yang Status bukan "Selesai"
  - Kirim ringkasan ke Telegram berisi daftar tugas & diskusi yang masih belum dikerjakan.
  - DILEWATI jika [STATUS] Stop aktif di Notion.

===================================================================
ANTI-DUPLIKASI (State Management)
===================================================================

Karena GitHub Actions bersifat ephemeral (file hilang setiap run),
state disimpan permanen di Notion database.

Method cek di NotionService:
  sudahAda(nama)              → filter: Name == nama
                                Dipakai untuk [MATKUL] (nama sudah unik global)

  sudahAda(nama, namaMatkul)  → filter: Name == nama AND Mata Kuliah == namaMatkul
                                Dipakai untuk [DISKUSI] dan [TUGAS] agar
                                "Diskusi.1" dari matkul berbeda tidak
                                saling di-skip satu sama lain

  isStopAktif()               → cek apakah [STATUS] Stop ada di Notion
  simpanStatusStop()          → simpan [STATUS] Stop ke Notion
  hapusStatusStop()           → arsipkan (soft-delete) [STATUS] Stop dari Notion

===================================================================
API MOODLE YANG DIGUNAKAN
===================================================================

1. TOKEN / LOGIN
   GET /login/token.php
       ?username={NIM}&password={PASS}&service=moodle_mobile_app
   Response: { "token": "xxx" }

2. SITE INFO / USER ID
   GET /webservice/rest/server.php
       ?wsfunction=core_webservice_get_site_info&wstoken={TOKEN}&moodlewsrestformat=json
   Response: { "userid": 12345, ... }

3. DAFTAR MATKUL
   GET /webservice/rest/server.php
       ?wsfunction=core_enrol_get_users_courses&userid={ID}&wstoken={TOKEN}&moodlewsrestformat=json
   Response: [ { "id": courseId, "fullname": "Nama Matkul", ... } ]

4. ASSIGNMENT / TUGAS
   GET /webservice/rest/server.php
       ?wsfunction=mod_assign_get_assignments
       &courseids[0]={ID}&courseids[1]={ID}...
   Response: { "courses": [ { "id": courseId, "assignments": [ { "name": "...", "cmid": ..., "duedate": ... } ] } ] }

5. SUBMISSION STATUS TUGAS (fallback)
   GET /webservice/rest/server.php
       ?wsfunction=mod_assign_get_submission_status
       &assignid={ID}&userid={USER_ID}&wstoken={TOKEN}&moodlewsrestformat=json
   Response: { "lastattempt": { "submission": { "status": "submitted" | "..." } } }

6. FORUM LIST
   GET /webservice/rest/server.php
       ?wsfunction=mod_forum_get_forums_by_courses
       &courseids[0]={ID}&courseids[1]={ID}...
   Response: [ { "id": forumId, "cmid": courseModuleId, "course": courseId, "name": "..." } ]
   NOTE: "cmid" = Course Module ID, unik per aktivitas, kunci untuk completion lookup

7. DISKUSI PER FORUM
   GET /webservice/rest/server.php
       ?wsfunction=mod_forum_get_forum_discussions&forumid={ID}&wstoken={TOKEN}&moodlewsrestformat=json
   Response: { "discussions": [ { "id": discussionId, "name": "Diskusi.1" } ] }

8. ACTIVITY COMPLETION STATUS ← KUNCI DETEKSI "SUDAH DIKERJAKAN"
   GET /webservice/rest/server.php
       ?wsfunction=core_completion_get_activities_completion_status
       &courseid={ID}&userid={USER_ID}&wstoken={TOKEN}&moodlewsrestformat=json
   Response: { "statuses": [ { "cmid": 123, "state": 1, ... } ] }
   State values:
     0 = Belum selesai
     1 = Selesai (complete)
     2 = Selesai dengan nilai lulus (complete pass)
     3 = Selesai dengan nilai gagal (complete fail)
   NOTE: Ini adalah sumber yang SAMA dengan tanda hijau "✓ Done" di UI Moodle

9. POST DISKUSI (fallback)
   GET /webservice/rest/server.php
       ?wsfunction=mod_forum_get_discussion_posts&discussionid={ID}&wstoken={TOKEN}&moodlewsrestformat=json
   Response: { "posts": [ { "userid": 12345, "message": "..." } ] }

10. COURSE CONTENTS (deteksi Praktik vs Reguler)
    GET /webservice/rest/server.php
        ?wsfunction=core_course_get_contents&courseid={ID}
        &options[0][name]=excludemodules&options[0][value]=1
    Response: [ { "name": "Sesi 1" } ] atau [ { "name": "Aktivitas Belajar 1" } ]
    NOTE: Dipakai untuk otomatis mendeteksi tipe matkul

11. PESAN MASUK (Messaging API)
    GET /webservice/rest/server.php
        ?wsfunction=core_message_get_conversations
        &userid={ID}&type=1&limitnum=50&wstoken={TOKEN}&moodlewsrestformat=json
    Response: { "conversations": [ { "unreadcount": 1, "members": [...], "messages": [...] } ] }

===================================================================
API NOTION YANG DIGUNAKAN
===================================================================

1. QUERY DATABASE (cek duplikasi & ambil pending)
   POST https://api.notion.com/v1/databases/{database_id}/query
   Headers: Authorization, Content-Type, Notion-Version: 2022-06-28
   Body: { "filter": { ... } }

2. BUAT HALAMAN BARU
   POST https://api.notion.com/v1/pages
   Body: { "parent": { "database_id": "..." }, "properties": { "Name": {...}, "Mata Kuliah": {...} } }
   Response includes: { "id": "page_id" }  ← disimpan untuk update status

3. UPDATE HALAMAN (tandai Selesai)
   PATCH https://api.notion.com/v1/pages/{page_id}
   Body: { "properties": { "Status": { "status": { "name": "Selesai" } } } }
   NOTE: Gunakan property type "status", bukan "select"

4. ARSIPKAN HALAMAN (hapus status marker)
   PATCH https://api.notion.com/v1/pages/{page_id}
   Body: { "archived": true }
   Dipakai untuk menghapus [STATUS] Stop dan [STATUS] Matkul Komplit

===================================================================
ENVIRONMENT VARIABLES
===================================================================

Variabel yang WAJIB ada (di .env lokal atau GitHub Secrets):

  UT_NIM              Nomor Induk Mahasiswa UT
  UT_PASS             Password e-learning UT
  TELEGRAM_BOT_TOKEN  Token bot Telegram (dari @BotFather)
  TELEGRAM_CHAT_ID    Chat ID tujuan notifikasi
  NOTION_TOKEN        Notion Integration Token
  NOTION_DATABASE_ID  ID database Notion

===================================================================
GITHUB ACTIONS WORKFLOW
===================================================================

File: .github/workflows/jadwal-bot.yml

Jadwal: cron "0 */4 * * *" = setiap 4 jam sekali
        Setara: 07:00, 11:00, 15:00, 19:00, 23:00, 03:00 WIB (UTC+7)

Trigger manual: Settings → Actions → "Auto Bot Tracker UT" → Run workflow

Step yang dijalankan:
  1. actions/checkout@v4         → Clone kode terbaru
  2. actions/setup-java@v4       → Setup Java 25 (Temurin) + cache Maven
  3. Bikin File .env             → Inject secrets ke file .env
  4. mvn clean package           → Compile & build JAR
  5. java -jar *-jar-with-dependencies.jar → Jalankan bot

===================================================================
STRUKTUR DATABASE NOTION
===================================================================

Property yang diperlukan:
  Name       | Title  | Nama entri (dengan prefix)
  Mata Kuliah| Select | Nama mata kuliah
  Status     | Status | Status pengerjaan

Nilai Status yang dipakai:
  "Belum dimulai"  → Default saat entry baru dibuat
  "Selesai"        → Di-update otomatis bot saat activity completion state >= 1

Format prefix Name:
  [MATKUL] Nama Matkul           → Mata kuliah aktif
  [TUGAS] Nama Tugas             → Tugas dari Assignment API
  [DISKUSI] Diskusi.1            → Diskusi forum
  [DISKUSI] Kehadiran Sesi ke-1  → Forum kehadiran
  [STATUS] Stop                  → Penanda laporan dinonaktifkan (Mata Kuliah: SYSTEM)
  [STATUS] Matkul Komplit        → Penanda matkul sudah komplit (Mata Kuliah: SYSTEM)

===================================================================
CATATAN PENTING & GOTCHA
===================================================================

1. FILE .env HARUS ADA DI .gitignore
   Pastikan baris ".env" ada di .gitignore. Jika tidak, kredensial
   bisa bocor ke GitHub history.

2. PROPERTY STATUS DI NOTION HARUS TIPE "STATUS"
   Bukan "Select". Jika pakai Select, PATCH request untuk update
   status akan gagal karena format JSON berbeda.
   - Status property: { "status": { "name": "Selesai" } }
   - Select property: { "select": { "name": "Selesai" } }

3. COMPLETION API MEMBUTUHKAN USER ID
   core_completion_get_activities_completion_status memerlukan
   userid parameter, bukan hanya token. Pastikan getUserId() berhasil.

4. FORUM CMID MUNGKIN TIDAK ADA
   Jika mod_forum_get_forums_by_courses tidak mengembalikan field "cmid",
   bot otomatis fallback ke cek via mod_forum_get_discussion_posts.
   Cek log "completion state" di GitHub Actions output untuk debug.

5. NOTION TOKEN EXPIRED
   Jika Notion error 401, generate ulang token di notion.so/my-integrations
   dan update GitHub Secret NOTION_TOKEN.

6. DUPLIKASI NOTIF TELEGRAM DARI MASA LALU
   Jika ada entry di Notion dari sebelum perubahan ini, dan entry tersebut
   tidak punya prefix [DISKUSI], maka diskusi dengan nama yang sama akan
   dianggap baru dan dinotif lagi. Solve: hapus atau beri prefix manual.

7. [STATUS] Stop TIDAK OTOMATIS HILANG SAAT SEMESTER BERAKHIR
   Jika user kirim "Stop" dan semester berakhir (Moodle kosong), bot tidur
   tanpa menghapus [STATUS] Stop. Stop tetap aktif sampai user kirim "Run"
   atau ada matkul baru terdeteksi di semester berikutnya (auto-resume).

===================================================================
RIWAYAT PERUBAHAN
===================================================================

[v1.0] - App.java (satu file)
  - Login ke Moodle, ambil daftar matkul
  - Kirim semua ke Notion & Telegram
  - State management: file lokal sent_tasks.txt
  - MASALAH: file tidak persisten di GitHub Actions (reset tiap run)
  - Kode backup tersimpan di App.txt

[v2.0] - App.java (refactoring)
  - State management dipindah ke Notion API (sudahAdaDiNotion)
  - JSON dibangun pakai JSONObject (hindari error karakter khusus)
  - Satu HttpClient dipakai ulang (singleton, thread-safe)
  - BASE_URL jadi konstanta
  - Notif startup Telegram dihapus (spam)

[v3.0] - Dipecah 4 file + Forum Diskusi
  - Struktur: App.java, MoodleService.java, NotionService.java, TelegramService.java
  - Tambah cekDiskusiBaru() via mod_forum_get_forums_by_courses
  - Filter isForumRelevan(): hanya Diskusi/Kehadiran/Tugas
  - Fix duplikasi: cek Name + Mata Kuliah (bukan Name saja)
  - Notif Telegram format rapi dengan nama matkul per item

[v4.0] - Completion API + Laporan Periodik
  - Ganti sudahBerpartisipasi() (cek posts) →
    getCompletionStatus() (Activity Completion API = tanda hijau Done)
  - Completion map dibangun per-course (1 API call vs N API calls)
  - getPageId() + tandaiSelesai(): update status Notion otomatis
  - simpanDanAmbilId(): simpan page dan langsung dapatkan ID-nya
  - Tambah kirimRingkasanPeriodik(): laporan ke Telegram setiap run
  - Tambah getPendingDiskusi(): query Notion untuk item belum Selesai
  - Format notif lebih informatif (nama matkul di setiap item)

[v5.0] - Penanganan Sesi Akhir & Akhir Semester
  - Penapisan notifikasi periodik Telegram pada Sesi 8 / Aktivitas Belajar 15 saat semua tugas selesai.
  - Reset otomatis status dan penghentian notifikasi periodik saat semester berakhir (mata kuliah kosong).
  - Integrasi perintah Telegram "Matkul Komplit!" untuk menonaktifkan notifikasi penambahan mata kuliah secara dinamis.

[v6.0] - Sistem Trigger Word Stop/Run (menggantikan deteksi Sesi Akhir)
  - DIHAPUS: Seluruh logika deteksi pola Sesi 8 / Aktivitas Belajar 15
    (checkEndSessionPattern, hasReachedEndSession, isEndSessionReached).
    Deteksi ini rentan false-positive dan tidak reliabel karena bergantung
    pada pola nama yang bisa berubah sewaktu-waktu.
  - DIHAPUS: Keyword Telegram "Matkul Komplit!" — digantikan oleh sistem
    trigger word yang lebih fleksibel.
  - BARU: Trigger word "Stop" via Telegram.
    Menyimpan [STATUS] Stop ke Notion. Selama aktif, bot berhenti mengirim
    laporan periodik namun tetap mengecek elearning setiap run.
  - BARU: Trigger word "Run" via Telegram.
    Menghapus [STATUS] Stop dari Notion. Laporan periodik aktif kembali.
  - BARU: Auto-resume otomatis saat matkul baru terdeteksi.
    Jika [STATUS] Stop aktif namun elearning menambahkan matkul baru
    (semester baru dimulai), bot secara otomatis menghapus [STATUS] Stop
    dan mengirimkan notifikasi tanpa perlu intervensi manual dari user.
  - UPDATE: Ganti API tugas dari Calendar API → mod_assign_get_assignments
    agar lebih akurat dan tidak bergantung pada event kalender.
  - UPDATE: Jadwal GitHub Actions diperbarui dari tiap 6 jam → tiap 4 jam.
  - UPDATE: Tambah logging Telegram (status HTTP response) untuk debugging.
  - UPDATE: Tambah logging detail di TelegramService.kirim() untuk memudahkan
    debug saat pesan tidak terkirim.

[v6.1] - Fix Bug: Keyword Telegram Dideteksi Ganda Antar Run
  - BUG: Bot mendeteksi keyword "Stop" lama dari run sebelumnya bersamaan dengan
    keyword "Run" baru, sehingga dalam satu run muncul "Bot dihentikan" lalu
    langsung "Bot diaktifkan kembali" dan laporan tetap terkirim.
  - ROOT CAUSE: cekKeyword() menggunakan /getUpdates tanpa offset, sehingga
    Telegram mengembalikan semua pesan lama yang belum di-acknowledge.
    Dua panggilan cekKeyword() terpisah untuk "Stop" dan "Run" masing-masing
    membaca ulang seluruh history yang sama.
  - FIX: Ganti cekKeyword() dengan ambilDanAkuiKeyword() yang:
    1. Membaca SEMUA keyword sekaligus dalam satu panggilan /getUpdates.
    2. Langsung acknowledge semua update dengan /getUpdates?offset={max_id+1}
       sehingga pesan lama tidak muncul di run berikutnya.
    3. Memproses keyword secara berurutan sesuai urutan pesan (last command wins).
  - PERUBAHAN FILE: TelegramService.java (method baru), App.java (logika baru).
